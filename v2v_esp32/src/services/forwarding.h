namespace Service{
    class Forwarding
    {
    private:
        /* data */
    public:
        Forwarding(/* args */);
        ~Forwarding();
        // Berechnet Drehung und Fahrtrichtung
        // Reicht diese Informationen an den Positioning Service weiter
    };
    
  

}