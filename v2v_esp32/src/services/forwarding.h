#include <Arduino.h>
#pragma once

namespace Service{
    class Forwarding
    {
    private:
        /* data */
    public:
        Forwarding(/* args */);
        ~Forwarding();
        String calculateNextMovement();
    };
    
  

}